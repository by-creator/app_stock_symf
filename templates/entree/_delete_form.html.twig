<form method="post" action="{{ path('app_entree_delete', {'id': entree.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette entrée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entree.id) }}">
    <button class="btn">SUPPRIMER</button>
</form>
