
<!DOCTYPE html>
<html lang="en">
<head>
<title>ENTRY</title>
{{include ('layouts/login/head.html.twig')}}
{{include ('layouts/site/head.html.twig')}}
</head>
<body>
{{include ('layouts/site/spinner.html.twig')}}
{{include ('layouts/site/navbar.html.twig')}}
<div class="limiter">
		<div class="container-login100">
			<div class="wrap-login100">
					<span class="login100-form-logo">
						<i class="fa fa-3x fa-laptop-code ">ENTRÉE</i>
					</span>

					<span class="login100-form-title p-b-34 p-t-27">
						ENTRÉE SELECTIONNÉE
					</span>
<div class="wrap-input100 validate-input">

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ entree.id }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ entree.produit }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entree.dateE ? entree.dateE|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ entree.qteE }}</td>
            </tr>
        </tbody>
    </table>

   <button class="btn">
    <a href="{{ path('app_entree_edit', {'id': entree.id}) }}">APPORTER DES MODIFICATIONS</a>
</button>
    </br>
    <button class="btn">
     <a href="{{ path('app_entree_index') }}">REVENIR À LA LISTE</a>
     </button>
</div>
                
			</div>
            {{ include ('layouts/site/newsletter.html.twig') }}
            {{ include ('layouts/site/footer.html.twig') }}
		</div>
        
	</div>
{{include ('layouts/login/js.html.twig')}}
{{include ('layouts/site/js.html.twig')}}

</body>
</html>
