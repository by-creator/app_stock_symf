 <!-- Navbar & Hero Start -->
        <div class="container-xxl position-relative p-0">
            <nav class="navbar navbar-expand-lg navbar-light px-4 px-lg-5 py-3 py-lg-0">
                <a href="{{path('app_accueil_index')}}" class="navbar-brand p-0">
                    <h1 class="m-0">APP STOCK</h1>
                    <!-- <img src="{{asset('../site/img/logo.png')}}" alt="Logo"> -->
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                    <span class="fa fa-bars"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <div class="navbar-nav mx-auto py-0">
                        <a href="{{path('app_accueil_index')}}" class="nav-item nav-link active">ACCUEIL</a>
                        <a href="{{path('app_categorie_index')}}" class="nav-item nav-link">CATEGORIES</a>
                        <a href="{{path('app_produit_index')}}" class="nav-item nav-link">PRODUITS</a>
                        <a href="{{path('app_entree_index')}}" class="nav-item nav-link">ENTRÉES</a>
                        <a href="{{path('app_sortie_index')}}" class="nav-item nav-link">SORTIES</a>
                    </div>
                    <a href="{{path('app_logins')}}" class="btn rounded-pill py-2 px-4 ms-3 d-none d-lg-block">DECONNEXION</a>
                </div>
            </nav>

            <div class="container-xxl bg-primary hero-header">
                <div class="container px-lg-5">
                    <div class="row g-5 align-items-end">
                        <div class="col-lg-6 text-center text-lg-start">
                            <h1 class="text-white mb-4 animated slideInDown">Bienvenu(e) sur APP STOCK</h1>
                            <p class="text-white pb-3 animated slideInDown">
                            Votre plateforme de gestion de stock. Ici vous pouvez gérer vos catégories, les produits appartenant à chaque catégories, mais également
                            gérer les entrées et sorties de chaque produits à une date bien définie.
                            </p>
                            <a href="" class="btn btn-secondary py-sm-3 px-sm-5 rounded-pill me-3 animated slideInLeft">EN SAVAOIR PLUS</a>
                            <a href="" class="btn btn-light py-sm-3 px-sm-5 rounded-pill animated slideInRight">CONTACTEZ-NOUS</a>
                        </div>
                        <div class="col-lg-6 text-center text-lg-start">
                            <img class="img-fluid animated zoomIn" src="{{asset('../site/img/hero.png')}}" alt="">
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Navbar & Hero End -->